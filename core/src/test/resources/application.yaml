resource-path: ${user.home}${file.separator}spring-jpa-rest-board-example-resource${file.separator}

spring:
  profiles: test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: testAdmin
    password: testPassword

logging:
  level:
    devcrema: debug
    org:
      hibernate: info
      springframework:
        security: info